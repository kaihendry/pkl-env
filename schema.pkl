open module demo

typealias ENV = "dev"|"int"|"uat"|"prd"

class OutputPort {
    name: String
    Mapping<ENV, Deployment>
}

class Deployment {
    bucketName: String
}

outputs: Listing<OutputPort>
